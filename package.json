{
  "name": "vmix-control-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Local web control panel for vMix HTTP API.",
  "scripts": {
    "start": "node server.js",
    "bridge": "node bridge-client.js",
    "check": "node --check server.js && node --check bridge-client.js && node --check public/app.js"
  }
}
